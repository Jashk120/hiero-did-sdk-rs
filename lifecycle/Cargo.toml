[package]
name = "hiero-did-lifecycle"
version.workspace = true
edition.workspace = true
repository.workspace = true
license.workspace = true
description = "State machine and lifecycle engine driving did:hedera create/update/deactivate operations"
keywords = ["did", "hedera", "lifecycle", "state-machine"]
categories = ["cryptography", "data-structures"]

[dependencies]
hiero-did-core = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
